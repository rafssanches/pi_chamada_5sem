<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rafsanches.com.br.pi_chamada">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfessorMateriaActivity"></activity>
        <activity
            android:name=".ProfessorListaMateriasActivity"
            android:label="@string/title_activity_professor_lista_materias"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfessorCalendarActivity" />
        <activity android:name=".ListaMateriasActivity" />
        <activity android:name=".AlunoCalendarActivity" />
        <activity android:name=".AlunoMateriasActivity" />
        <activity android:name=".ProfessorQRCodeActivity" />
        <activity android:name=".ProfessorTurmaActivity" />
        <activity android:name=".ProfessorActivity" />
        <activity android:name=".AlunoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>